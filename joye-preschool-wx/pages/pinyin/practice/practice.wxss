/* 拼音练习页面样式 */

.container {
  padding: 30rpx;
  min-height: 100vh;
  box-sizing: border-box;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin: 30rpx 0 20rpx;
}

/* 题型选择 */
.type-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  margin-bottom: 30rpx;
}

.type-card {
  background: #fff;
  border: 4rpx solid #E8E8E8;
  border-radius: 20rpx;
  padding: 30rpx;
  text-align: center;
  transition: all 0.2s;
}

.type-card.selected {
  border-color: #87CEEB;
  background: #F0F8FF;
}

.type-card:active {
  transform: scale(0.96);
}

.type-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
}

.type-name {
  font-size: 28rpx;
  font-weight: 500;
  color: #333;
}

/* 数量选择 */
.count-grid {
  display: flex;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.count-btn {
  flex: 1;
  padding: 24rpx;
  background: #fff;
  border: 4rpx solid #E8E8E8;
  border-radius: 16rpx;
  text-align: center;
  font-size: 28rpx;
  color: #666;
}

.count-btn.selected {
  border-color: #87CEEB;
  background: #F0F8FF;
  color: #4A90D9;
  font-weight: 600;
}

/* 按钮 */
.btn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 24rpx 40rpx;
  border-radius: 50rpx;
  font-size: 30rpx;
  font-weight: 500;
}

.btn-primary {
  background: linear-gradient(135deg, #87CEEB, #6BB3D9);
  color: white;
}

.btn-outline {
  background: transparent;
  border: 2rpx solid #87CEEB;
  color: #4A90D9;
}

.btn-large {
  padding: 32rpx 60rpx;
  font-size: 34rpx;
}

.btn-block {
  width: 100%;
}

/* 进度头部 */
.progress-header {
  margin-bottom: 30rpx;
}

.progress-header text {
  font-size: 26rpx;
  color: #999;
  margin-bottom: 12rpx;
  display: block;
}

.progress-bar {
  height: 16rpx;
  background: #E8E8E8;
  border-radius: 8rpx;
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #87CEEB, #6BB3D9);
  border-radius: 8rpx;
  transition: width 0.3s;
}

/* 题目卡片 */
.question-card {
  background: #fff;
  border-radius: 32rpx;
  padding: 50rpx 40rpx;
  text-align: center;
  margin-bottom: 40rpx;
  box-shadow: 0 8rpx 32rpx rgba(135, 206, 235, 0.2);
}

.question-label {
  font-size: 26rpx;
  color: #999;
  margin-bottom: 20rpx;
}

.question-text {
  font-size: 64rpx;
  font-weight: 700;
  color: #333;
  margin-bottom: 20rpx;
}

.question-text.pinyin-text {
  color: #E74C3C;
}

.sound-hint {
  font-size: 24rpx;
  color: #87CEEB;
}

/* 选项网格 */
.options-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.option-btn {
  background: #fff;
  border: 4rpx solid #E8E8E8;
  border-radius: 20rpx;
  padding: 36rpx;
  text-align: center;
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  transition: all 0.2s;
}

.option-btn:active {
  transform: scale(0.96);
}

.option-btn.selected {
  border-color: #87CEEB;
  background: #F0F8FF;
}

.option-btn.correct {
  border-color: #98D8AA;
  background: rgba(152, 216, 170, 0.3);
  color: #2D7A4D;
}

.option-btn.wrong {
  border-color: #FF6B6B;
  background: rgba(255, 107, 107, 0.3);
  color: #C0392B;
}

/* 结果卡片 */
.result-card {
  background: #fff;
  border-radius: 24rpx;
  text-align: center;
  padding: 60rpx 40rpx;
  margin-top: 40rpx;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.05);
}

.result-icon {
  font-size: 100rpx;
  margin-bottom: 20rpx;
}

.result-score {
  font-size: 80rpx;
  font-weight: 700;
  color: #4A90D9;
  margin-bottom: 20rpx;
}

.result-text {
  font-size: 32rpx;
  color: #666;
  margin-bottom: 30rpx;
}

.stars {
  display: flex;
  justify-content: center;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.star {
  font-size: 60rpx;
  opacity: 0.3;
}

.star.active {
  opacity: 1;
}

.result-buttons {
  display: flex;
  gap: 20rpx;
}

.result-buttons .btn {
  flex: 1;
}
